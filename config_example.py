app_name = 'Logging Server'
flask = dict(
        debug=True
)
db = dict(
        host='localhost',
        credentials=dict(
                username='username',
                password='password',
        ),
        db_name='dbName',
)
